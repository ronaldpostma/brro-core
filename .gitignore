# Local reference copies (repo root only)
/ref/

# Versioned ZIP builds — keep locally, not in Git (repo root only)
/versions/

# Release zip at plugin root
/brro-core.zip
/brro-core-*.zip

# Cursor / VS Code workspace data
.cursor/
.vscode/